var y = "it goes on"
var andonify = fn(s, i, b) -> res {
    res = s
    if false
        return
    end
    while i > 0
        i = i - 1
        res = res + " and on"
        res
    end
}
y = andonify(y, 10, false)
y
